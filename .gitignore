.venv/
__pycache__/
*.pyc
.env
instance/
flask_session/
*.egg-info/
.DS_Store
watranscribe.egg-info/

# Legacy Streamlit source archive — kept on disk for reference, not tracked.
WAtranscribe-claude-public-app-clone-v1-u1ubpf.zip

# watranscribe-bot and watranscribe-twa live here on disk but are each their
# own git repo/GitHub project — deliberately isolated (own deploy unit/
# secrets or own signing key), not part of this repo.
watranscribe-bot/
watranscribe-twa/
